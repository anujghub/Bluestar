trigger Notes on ContentDocumentLink (after insert) {

/* for (ContentDocumentLink cdoc :Trigger.new)
    {
      if(cdoc.LinkedEntityId <> NULL)
       {Account acct;
        acct.id = cdoc.LinkedEntityId ;
        if(acct.Parent.id<> NULL)
        {cdoc.LinkedEntityId = acct.Parent.id ;
        insert cdoc;}}
        
        
    }*/
    
}